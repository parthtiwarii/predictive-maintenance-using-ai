If you prefer a Jupyter notebook, open a new notebook and copy the contents of `src/evaluate.py`
into cells, or run the script from a notebook cell using:
```
!python src/evaluate.py --data data/sample_data.csv --out report
```
